package com.slideindex.app.gesture

/**
 * Placeholder for the `:core:gesture` module.
 *
 * Gesture domain types currently live in `:app` until AppSettings / overlay
 * dependencies are extracted. New pure gesture logic should land here first.
 */
object GestureModule
